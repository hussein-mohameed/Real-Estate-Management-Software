/**
 * ═══════════════════════════════════════════════════════════════════════
 *  السمة — ثوابت مشتركة بين النصّ الحاجب ومكوّن التبديل.
 * ═══════════════════════════════════════════════════════════════════════
 *
 * ⚠️ **هذا الملف بلا `"use client"` وبلا `"server-only"` عمداً.** يقرأه
 * الاثنان: النصّ الذي يُحقن في الصفحة من الخادم، والزرّ الذي يعمل في
 * المتصفّح. ومفتاح تخزين مكتوب مرّتين ينتج سمةً تُحفَظ ولا تُقرأ.
 */

/** مفتاح `localStorage`. مُنَطَّق بالمشروع كي لا يتعارض مع تطبيق آخر على نفس المنفذ. */
export const THEME_KEY = "compound.theme";

/**
 * اختيار المستخدم — **ثلاث حالات لا اثنتان.**
 *
 * ⚠️ زرٌّ بحالتين يُتلف تفضيل النظام: أول نقرة تثبّت اختياراً صريحاً
 * ولا سبيل للعودة إلى «اتبع نظامي». و«حسب النظام» ليست قيمةً تُخزَّن
 * بل **غياب** قيمة — فمسح المفتاح هو ما يعيد الاتباع.
 */
export type ThemeChoice = "light" | "dark" | "system";

/** ما يُطبَّق فعلاً بعد حلّ «حسب النظام». */
export type ResolvedTheme = "light" | "dark";
